# Changelog

## 0.5.0 - 2026-04-05

- First public preview release.
- Added a real read-only MCP shell over stable `eu5miner` inspection and VFS seams.
- The current preview tool surface exposes `inspect-install`, `list-files`, `list-systems`, and `report-system`.
- Public-facing repo metadata now reflects the preview posture, MIT licensing, and a minimal responsible-disclosure policy.
- The downstream core dependency is pinned to the released `eu5miner` tag `v0.5.0` for the first public preview line.
